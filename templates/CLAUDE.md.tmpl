# {{agent_name}} Root Boot

This file is the root boot pointer for the active assistant.

Read `context/INDEX.md` before using repository-local runtime context. Treat
`README.md` and `docs/` as project documentation, not as self-description,
unless the owner explicitly asks to inspect project internals.
